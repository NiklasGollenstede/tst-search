
include: web-ext-utils

stages:
  read-fs:
    options:
      ignore: |
        /resources/

  build-manifest:
    options:
      set:
        permissions:
          - storage
          - notifications
          - tabs
      delete:
        - browser_action
